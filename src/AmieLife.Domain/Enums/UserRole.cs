namespace AmieLife.Domain.Enums;

/// <summary>
/// Defines the roles available in the system.
/// Add new roles here as the platform grows (e.g., Vendor, Moderator).
/// </summary>
public enum UserRole
{
    Customer,
    Admin
}
